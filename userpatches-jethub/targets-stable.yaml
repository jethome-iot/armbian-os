common-gha-configs:
  armbian-gha: &armbian-gha
    runners:
      default: "ubuntu-latest" # or a ["tag", "list"]
      by-name:
        kernel: [ "self-hosted", "Linux" ]
        uboot: [ "self-hosted", "Linux" ]
        armbian-bsp-cli: [ "ubuntu-latest" ]
      by-name-and-arch:
        rootfs-armhf: [ "ubuntu-latest" ]
        rootfs-arm64: [ "ubuntu-latest" ]
        rootfs-amd64: [ "ubuntu-latest" ]
        rootfs-riscv64: [ "ubuntu-latest" ]
        image-armhf: [ "ubuntu-latest" ]
        image-arm64: [ "ubuntu-latest" ]
        image-amd64: [ "ubuntu-latest" ]
        image-riscv64: [ "ubuntu-latest" ]

lists:

  headless: &headless

    - { BOARD: jethubj100, BRANCH: current }
    - { BOARD: jethubj80, BRANCH: current }

targets:

  full-cli-stable-debian:
    enabled: yes
    configs: [ jethome-images ]
    pipeline:
      gha: *armbian-gha
      build-image: "yes"
    vars:
      RELEASE: bookworm
      BUILD_MINIMAL: "no"
      BUILD_DESKTOP: "no"
    items:
      - *headless

  full-cli-oldstable-debian:
    enabled: yes
    configs: [ jethome-images ]
    pipeline:
      gha: *armbian-gha
      build-image: "yes"
    vars:
      RELEASE: bullseye
      BUILD_MINIMAL: "no"
      BUILD_DESKTOP: "no"
    items:
      - *headless

  full-cli-stable-ubuntu:
    enabled: yes
    configs: [ jethome-images ]
    pipeline:
      gha: *armbian-gha
      build-image: "yes"
    vars:
      RELEASE: jammy
      BUILD_MINIMAL: "no"
      BUILD_DESKTOP: "no"
    items:
      - *headless

  full-cli-release-ubuntu:
    enabled: yes
    configs: [ jethome-images ]
    pipeline:
      gha: *armbian-gha
      build-image: "yes"
    vars:
      RELEASE: lunar
      BUILD_MINIMAL: "no"
      BUILD_DESKTOP: "no"
    items:
      - *headless

